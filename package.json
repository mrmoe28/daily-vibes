{
  "name": "daily-vibe",
  "version": "1.0.0",
  "description": "A web application built with the backend template",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p data && mkdir -p uploads",
    "vercel-build": "mkdir -p data && mkdir -p uploads && echo 'Build completed for Vercel'",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "lint": "echo 'Linting not configured yet'",
    "update-schema": "node scripts/update-schema.js"
  },
  "dependencies": {
    "@corsaro-creative/super-react": "^0.2.0",
    "@neondatabase/serverless": "^1.0.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "jsdom": "^26.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.16.3",
    "sqlite3": "^5.1.6",
    "super-design": "^0.1.12",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^20.8.0",
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "backend",
    "template",
    "express",
    "api",
    "authentication",
    "database"
  ],
  "author": "mrmoe28",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mrmoe28/daily-vibe.git"
  }
}
